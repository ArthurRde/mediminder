# Optional: nur nötig, um die Dev-Defaults zu überschreiben (z. B. für ein Deployment).
# Kopie als .env ablegen, sie wird von docker compose und dem Backend gelesen.
POSTGRES_DB=mediminder
POSTGRES_USER=mediminder
POSTGRES_PASSWORD=mediminder
# Erzeugen mit: openssl rand -base64 48
# MEDIMINDER_JWT_SECRET=
